import 'package:flutter/material.dart';

class WelomeScreen extends StatelessWidget {
  const WelomeScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(body: Center(child: Text("Welcome Screen")));
  }
}
